package section1ProgrammingBasics.stringTemplates.exercises

/*
You can search for and replace any text in a file.
Press &shortcut:Find; for Find and
&shortcut:Replace; for Replace.

Replace all instances of foo in the file with bar.

Note that you can press &shortcut:Find;,
start typing, then press &shortcut:Replace;
and the history is preserved.
Pressing &shortcut:Replace; adds a field
where you can type the replace string.

Click on the small arrow to the left of the typing window to see the search history.
*/

fun foo() = 1

fun main() {
    println(foo())
}

/* Output:
1
*/